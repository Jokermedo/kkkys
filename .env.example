########################################
# Public site config
########################################
# Important for absolute URLs in the app
NEXT_PUBLIC_SITE_URL=https://your-domain.com

########################################
# Database (Postgres)
########################################
# The app expects POSTGRES_URL (or DATABASE_URL) in lib/database/connection.ts
# For Supabase Postgres, use the connection string from Database settings (preferably the pooled connection string)
POSTGRES_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME
# Optional alias supported by some tools
# DATABASE_URL=

########################################
# Supabase configuration (if used elsewhere in the app)
########################################
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_ANON_KEY=YOUR_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

########################################
# Admin/auth settings
########################################
# Use a strong, long random string
JWT_SECRET=please-change-me
# Optional: override default admin password (fallback may be admin123123)
# ADMIN_PASSWORD=

########################################
# Optional local JSON storage fallback (unused when Postgres/Supabase is enabled)
########################################
# ORDERS_DB_PATH=.data/orders.json
